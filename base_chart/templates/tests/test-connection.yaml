apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "base_chart.name" . }}-test-connection"
  labels:
    {{- include "base_chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "base_chart.name" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
